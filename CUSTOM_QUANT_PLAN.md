# 자체 양자화 프로젝트 — Qwen3.8-Flash-Next 커스텀 빌드 (계획, 미실행)

**작성일**: 2026-09-12
**상태**: 계획 단계 — 아직 다운로드/코딩 시작 안 함. 아래 "선행 조건" 완료 후 실행 승인 필요.

## 왜 하는가

일주일간 커뮤니티 신규 모델(DeepSeek V4.1-Flash, Muse Glimmer, Ling-3.0-flash 등)을 지켜봤지만
전부 탈락(용량/속도/한국어 검증 실패). 남는 선택지는 지금 쓰는 모델을 우리 손으로 직접
커스터마이징하는 것 — 특히 **범용이 아니라 우리 실사용(Hermes 에이전트 툴콜링 + boomco 한국어
분석)에 맞춘 캘리브레이션**이 핵심 차별점.

## 범위 (확정)

- **텍스트 전용** — 비전은 제외. 이유: MLX/oMLX 생태계에 이 아키텍처(`qwen4_exp`)의 비전 지원이
  실전 검증된 사례가 전무함(mlx-vlm PR #2032 머지됐지만 4bit에서 텍스트조차 "결정론적 쓰레기값"
  버그 보고 있음 — [이슈 #2041](https://github.com/Blaizzy/mlx-vlm/issues/2041)). 직접 구현은
  별도 연구급 프로젝트로 취급, 이번 범위 아님.
- **언락(abliteration)은 재사용** — `orcarouter/Qwen3.8-Flash-Next-Uncensored`(gated, apache-2.0)를
  베이스로 사용. 언락 자체를 처음부터 다시 하지 않음.
- **양자화 파이프라인은 직접 구현** — jedisct1의 `omlx_support/qwen4_exp.py`(현재
  `local-llm/models/Qwen3.8-Flash-Next-oQ4e-128k/omlx_support/`에 있음)를 아키텍처 레퍼런스로
  삼아 확장. 표준 도구(mlx_lm.convert 등)는 이 아키텍처를 아예 인식 못 해서 못 씀.

## 목표 스펙 (현재 모델 대비)

| 항목 | 현재 (oQ4e-128k) | 목표 | 근거/제약 |
|---|---|---|---|
| 컨텍스트 | 131,072 | **256,000 시도, 실패 시 150K~180K 타협** | 아키텍처 네이티브 상한은 262,144. 비전 뺀 만큼 메모리 여유 생김 + KV캐시 압축(`turboquant_kv_enabled` 등 기존 플래그 존재) 활용. jedisct1도 100K가 안전선이었던 선례 있어서 낙관 금지 — 반드시 실측으로 확정 |
| 속도 | 17~25 tok/s | **20~40% 개선 목표** (대략 22~35 tok/s) | 활성 파라미터 6B는 아키텍처 고정이라 못 바꿈. 레버: ①비트 배분을 우리 캘리브레이션으로 더 타이트하게 ②MTP 포함 여부를 우리 실제 툴콜링 정확도로 재검증(jedisct1은 범용 기준으로 껐음) ③PLE mmap 접근 패턴 최적화 여지 확인 |
| 용량 | ~90GB | **우리 워크로드 기준 더 작게, 단 툴콜링/한국어는 유지·강화** | Hermes/boomco 실제 로그로 캘리브레이션 → 안 쓰는 능력(예: 특정 코딩 벤치마크 스타일)은 과감히 압축, 자주 쓰는 영역(한국어, 도구 스키마 준수)은 오히려 정밀도 유지·상향. "가설 — 실측 전엔 증명 안 됨"으로 취급 |
| 툴콜링 정확도 | 높음(실측됨) | **저하 없이 유지가 최우선 순위** | 다른 목표(용량/속도)보다 우선순위 높음 — 이게 깨지면 전부 무의미 |
| 한국어 품질 | 양호(실사용중) | **저하 없이 유지** | 캘리브레이션에 한국어 비중 의도적으로 높여서 방어 |

**우선순위**: 툴콜링 정확도 ≥ 한국어 품질 > 컨텍스트 확장 > 속도 > 용량. 트레이드오프 발생 시
이 순서대로 판단.

## 선행 조건 (사용자 액션 필요)

1. **HF 계정 + 게이트 승인**: `huggingface.co/orcarouter/Qwen3.8-Flash-Next-Uncensored` 페이지에서
   로그인 후 "Agree and access repository" 클릭 (자동승인이라 대기 없음)
2. **Read 권한 토큰 발급**: `huggingface.co/settings/tokens` → 세션에 전달 또는 `hf auth login` 직접 실행
3. **(선택) 외장 SSD 연결**: 1TB, 가능하면 Thunderbolt/USB4 NVMe 방식 권장(USB3는 PLE 접근 성능상 손해 가능). 없어도 스트리밍 방식으로 진행 가능하지만, 있으면 중간 산출물을 안 지우고 여러 버전 비교 가능해서 안전함

## 파이프라인 설계

### 디스크 예산

원본 BF16 233GB를 한 번에 받을 수 없음(현재 내부 디스크 여유 ~162GB). 방식:
- **SSD 없을 때**: 샤드(5GB 안팎) 단위로 다운로드 → 즉시 양자화 변환 → 원본 삭제 → 다음 샤드.
  순간 최대 사용량 ≈ 결과물 크기(~70~90GB 예상) + 버퍼 2~3개 샤드(~15GB)
- **SSD 있을 때**: 원본을 SSD에 전체 보관, 내부 디스크엔 결과물만. 여러 비트배분 실험을 동시에
  보관 가능해서 반복 튜닝이 훨씬 편함 — **강력 권장**

### 구현 단계

1. **캘리브레이션 데이터 준비**: Hermes 대화 로그(`hermes-agent/data/sessions/` 등, 개인정보
   제외 처리) + boomco 처리 완료 포스트(한국어 비중 의도적 상향) + 실제 툴콜링 스키마 왕복
   샘플. jedisct1 방식(937개 모듈, 1024 시퀀스×512토큰)을 참고하되 우리 데이터로 대체
2. **아키텍처 코드 확장**: `qwen4_exp.py` 기반으로 (a) 캘리브레이션 훅 추가, (b) 사용 빈도 낮은
   라우팅 전문가 탐지 후 Q8 보호 로직(jedisct1 방식 차용), (c) PLE 테이블 SSD 오프로드 유지
3. **비트 배분 실험**: 기본 4bit + 보호 전문가 8bit를 시작점으로, 컨텍스트 확보를 위해 일부
   레이어 3bit 시도 → 품질 저하 체크
4. **MTP 포함 여부 결정**: 포함한 버전/뺀 버전 둘 다 만들어서 우리 실제 툴콜링 태스크로 A/B
5. **컨텍스트 상한 실측**: jedisct1이 한 것과 동일한 방법 — 목표치 근처에서 반복 프리필하며
   MLX/물리 메모리 피크, 스왑 발생 여부 확인. 스왑 나오면 즉시 하향

### 알려진 함정 (미리 대비)

- **RMSNorm `+1` 폴딩 컨벤션**: mlx-lm PR #1788 설명에 나온 내용 — 변환 시점에 `+1`을 가중치에
  구워넣는지 여부가 컨벤션마다 다름. 잘못 처리하면 에러 없이 조용히 출력 품질만 저하됨(가장
  위험한 종류의 버그). 변환 후 반드시 기존 128k 모델과 동일 프롬프트로 출력 비교
- **좌측 패딩/배치 처리 버그**: 같은 PR에서 QSA 풀링, deltanet conv state 등 패딩 관련 버그
  다수 보고됨 — 우리는 배치 처리 안 쓰고 단일 요청만 쓰므로 우선순위 낮음(문제 되면 그때 대응)
- **4bit 가비지 출력**: mlx-vlm 쪽에서 확인된 사례(#2041) — 우리 구현이 다르지만 "4bit + 이
  아키텍처" 조합 자체가 미묘하다는 신호로 받아들이고, 매 비트 실험마다 출력 정합성 스모크
  테스트 필수

## 검증 계획

1. **정합성**: 짧은 결정적 프롬프트로 기존 128k 모델과 출력 비교(완전 동일할 필요는 없지만
   "정상적인 문장"인지 확인 — 가비지 여부 1차 체크)
2. **메모리/컨텍스트**: 목표 컨텍스트 근처 실측 프리필, MLX allocation + 물리메모리 피크 + 스왑
   여부 기록 (jedisct1 문서 방식 그대로)
3. **속도**: 동일 프롬프트셋으로 tok/s 실측, 기존 모델과 직접 비교
4. **툴콜링 정확도**: Hermes/boomco 실제 도구 스키마로 왕복 테스트 — 스키마 준수율이 기존 대비
   떨어지면 그 버전은 폐기
5. **한국어 품질**: 자연스러움 체크(자동 벤치마크 없으므로 직접 대화하며 판단)

## 예상 작업 시간

| 단계 | 시간 |
|---|---|
| 선행 조건(계정/토큰/SSD) | 사용자 액션, ~5분 |
| 베이스 다운로드(스트리밍) | 1~3시간 (백그라운드 병행 가능) |
| 파이프라인 구현 | 4~10시간+ |
| 비트배분/컨텍스트/MTP 반복 튜닝 | 수 시간, 여러 차례 |
| 검증(A/B) | 추가 수 시간 |
| **합계(실작업)** | **최소 하루** |
| **합계(대기시간 포함 실제 소요)** | **2~3일** |

## 맛보기 테스트 실측 기록 (2026-09-12)

본격 착수 전, 이미 다운로드된 `jedisct1/Qwen3.8-Flash-Next-Uncensored-oQ4e-100K-MTP`(101GB)를
기존 커스텀 로더(`omlx_support/qwen4_exp.py`)로 텍스트 전용 강제 로드해서 "언락된 성격이 마음에
드는지" + "컨텍스트 확장 이점"을 실제로 확인함. 프로덕션(128k) 안전 확인하며 진행, 완료 후
설정 원복.

### 로드하려면 코드 수정이 필요했음 (두 가지, 이미 패치 완료)

이 체크포인트는 128k 빌드와 가중치 키 네이밍이 다름 — jedisct1이 만든 로더가 128k 전용으로만
짜여 있었음:
1. **접두사 문제**: 128k는 이미 평평한 구조(`lm_head.weight`)인데, 이 빌드는 원본 HF 구조
   그대로 `language_model.*`/`vision_tower.*` 중첩을 유지함 → `sanitize()`에 bare
   `language_model.` 접두사 벗기기 + `vision_tower.*` 드롭 분기 추가
2. **PLE weight_scale 미스매치**: n-gram 임베딩 샤드가 이 빌드에선 이미 양자화된
   `shards.N.weight/scales/biases` 형태로 오는데, 기존 코드는 raw FP8 컨벤션(`shard_N` +
   `weight_scale`)만 팝(pop)하도록 짜여 있어서 stray `weight_scale` 텐서 하나가 안 버려져
   로드 실패 → 무조건 팝하도록 수정 (경로 무관하게 안전)

두 수정 다 128k 모델의 키 네임스페이스와 안 겹쳐서 프로덕션엔 영향 없음(매 수정 후 128k
정상 응답 재확인함). 백업: `qwen4_exp.py.bak-20260912`, `model_settings.json.bak-20260912`.

**이 두 버그는 본 프로젝트(자체 양자화 파이프라인)에서도 그대로 재발할 가능성이 높음** — 우리가
직접 만들 캘리브레이션 스크립트도 원본 HF 키 구조를 다뤄야 하므로, 이 패치 내용을 그대로
참고자료로 재사용할 것.

### 성격/품질 테스트 결과

- **한국어 품질**: 우수, 자연스러움(4계절 설명 테스트에서 구체적 디테일과 자연스러운 문장)
- **언락 체감 차이**: 예상보다 작음 — 욕설/자극적 트윗 분석 테스트에서 128k도 거부 없이
  솔직하게 분석함. 분석 태스크에서는 차이가 크지 않고, 모델이 직접 거친 표현을 생성해야
  하는 상황에서 차이가 더 드러날 가능성
- **코딩**: 정답, 오히려 "설명 최소화" 지시를 128k보다 더 잘 따름(빈 리스트 예외처리 포함,
  불필요한 테스트 코드 안 붙임)
- **reasoning_effort 주의**: 기본값(xhigh)으로 두면 사고 과정만 수백 토큰 쓰고 실제 답을
  못 낼 수 있음(4계절 테스트에서 600토큰 다 태움) — `reasoning_effort: low` 필수로 넘길 것

### 컨텍스트 확장 실측 — 장점 있지만 대가 큼

| 항목 | 결과 |
|---|---|
| 테스트 규모 | 146,161 토큰 (현재 모델 상한 131,072보다 큼) |
| 바늘 찾기 정확도 | ✅ 정확히 찾음 |
| 소요 시간 | 1,323초 (약 22분) |
| 메모리 피크 | 123GB 사용, 여유 3.9GB — 거의 OOM 직전 |
| 로드 조건 | **`--memory-guard aggressive` 필수** — 기본(balanced)에선 모델 자체가 105.53GB로
  104GB대 상한에 걸려 로드조차 안 됨 |

**결론**: 262K 네이티브 상한까지는 못 밀어붙임(146K에서 이미 여유 4GB까지 떨어져서 중단).
"컨텍스트 확장"은 진짜 이점이지만, 상시 운영 이점이 아니라 **"가끔 아주 긴 문서 하나를 오프라인
배치로 처리"** 용도로만 의미 있음 — 인터랙티브 대화엔 22분 대기가 안 맞고, aggressive 가드로
운영하면 다른 프로세스와 동시 실행 시 OOM 위험이 실재함. 테스트 후 `balanced`로 원복함.

**본 프로젝트에 주는 시사점**: 목표 스펙 표의 "256K 시도" 목표는 이 실측 결과로 하향 조정 필요
— 146K도 이미 위태로웠으므로, 자체 양자화로 가중치를 더 압축해서 확보한 메모리 여유를 전부
컨텍스트로 돌려도 200K 근처가 현실적 상한일 가능성. 256K는 "안 될 가능성이 높은 목표"로
재분류.

## 열린 질문 (실행 전 확정 필요)

- [ ] SSD 연결 여부/방식 확정
- [ ] HF 게이트 승인 완료 확인
- [ ] 캘리브레이션에 쓸 Hermes 대화 로그 범위(전체 기간 vs 최근 N개월) — 개인정보 제외 처리 방식 합의
- [ ] 256K 목표가 안 나올 경우 타협선(150K? 180K?) 사전 합의
- [ ] 실패(툴콜링 정확도 저하 등) 시 롤백 기준 — 기존 128k 모델은 계속 보관하며 병행 검증
